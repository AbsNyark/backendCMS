'use strict';

/**
 * nyark-creates-product router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::nyark-creates-product.nyark-creates-product');
