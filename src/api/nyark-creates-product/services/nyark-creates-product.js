'use strict';

/**
 * nyark-creates-product service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::nyark-creates-product.nyark-creates-product');
